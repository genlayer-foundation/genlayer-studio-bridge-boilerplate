# ===========================================
# GenLayer Configuration (for StringSender)
# ===========================================

# GenLayer RPC endpoint
GENLAYER_RPC_URL=https://studio-stage.genlayer.com/api

# Deployer private key (with 0x prefix)
PRIVATE_KEY=0x...

# BridgeSender contract address on GenLayer (deploy first if not exists)
BRIDGE_SENDER_ADDRESS=0x...

# ===========================================
# EVM Configuration (for StringReceiver)
# ===========================================

# Base Sepolia RPC URL
BASE_SEPOLIA_RPC_URL=https://sepolia.base.org

# BridgeReceiver address on Base Sepolia (must be deployed first)
BRIDGE_RECEIVER_ADDRESS=0x...

# Etherscan API key (optional, for contract verification)
ETHERSCAN_API_KEY=

# ===========================================
# Target Configuration
# ===========================================

# LayerZero Endpoint ID for Base Sepolia
TARGET_CHAIN_EID=40245

# StringReceiver address on Base Sepolia (set after deploying StringReceiver)
STRING_RECEIVER_ADDRESS=0x...
